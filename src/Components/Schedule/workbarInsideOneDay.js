const WorkbarInsideOneDay = ({ color, index }) => {
  return (
    <div
      style={{
        backgroundColor: `${color}`,
      }}
      className="workbar workbar--insideOneDay"
    ></div>
  );
};

export default WorkbarInsideOneDay;
